//STEP 1
// function halfNumber(x) {
//    var result = x / 2; 
//    window.document.write("Half of " + x + " is " + result);
// }
// halfNumber(5);

//STEP 2
// function squareNumber(x) {
//     var result = x * x;
//     window.document.write("The result of square the number " + x + " is " + result);
// }
// squareNumber(3);

//STEP 3
// function percentOf(x, y) {
//     var result = ((x/y) * 100);
//     window.document.write(x + " is " + result + "% of " + y);
// } 
// percentOf(2,4); 
//STEP 4
// function findModulus(x,y){
//     var result = x % y;
//     window.document.write(result + " is the modulus of " + y + " and " + x);
// }
// findModulus(10,4); 

// STEP 5
function exampleFnctn(x, y, z) {
    x = Number(window.prompt("Give me number x"));
    y = Number(window.prompt("Give me number y")); 
    z = Number(window.prompt("Give me number z"));4455
    var  result = x + y + z; 
    window.document.write("The sum of the x, y and z numbers is " + result);
}
exampleFnctn(); 

